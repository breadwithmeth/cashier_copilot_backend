import{create}from'zustand';import{persist}from'zustand/middleware';type S={accessToken:string|null;refreshToken:string|null;setTokens:(x:{accessToken:string;refreshToken:string})=>void;clear:()=>void};export const useAuth=create<S>()(persist(set=>({accessToken:null,refreshToken:null,setTokens:x=>set(x),clear:()=>set({accessToken:null,refreshToken:null})}),{name:'cashier-auth'}));

[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build -w @cashier/web"]

[start]
cmd = "npm run start -w @cashier/web"
